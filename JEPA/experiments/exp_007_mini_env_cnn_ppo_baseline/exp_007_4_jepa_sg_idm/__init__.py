# Reuse the exp_007_0_naive dashboard panel — same baseline metric columns,
# this variant just adds extras (jepa_loss, idm_loss, idm_accuracy,
# feat_std, feat_pairwise_l2, feat_effective_rank, grad_norm_encoder).
PANEL_EXPERIMENT = "exp_007_0_naive"
