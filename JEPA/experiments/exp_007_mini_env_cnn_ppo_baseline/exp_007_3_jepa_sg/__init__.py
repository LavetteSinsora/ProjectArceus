# Reuse the exp_007_0_naive dashboard panel — same baseline metrics columns,
# this variant just adds extras (jepa_loss, feat_std, feat_pairwise_l2,
# feat_effective_rank, grad_norm_encoder) that the panel will ignore.
PANEL_EXPERIMENT = "exp_007_0_naive"
