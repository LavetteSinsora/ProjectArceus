# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Test runner cache
.pytest_cache/

# Secrets
.env

# Experiment artifacts (large/regenerable — not for git)
JEPA/experiments/**/checkpoints/
JEPA/experiments/**/runs/

# Large/regenerable binary artifacts under other dir names (exp_008 buffers,
# exp_010 random data + pretrained encoders). Source + metrics stay tracked.
JEPA/experiments/**/data/
JEPA/experiments/**/jepa_runs/
JEPA/experiments/**/jepa_pretrained/
*.pt
*.npz

# Stale top-level checkpoints folder (no longer used)
JEPA/checkpoints/

# Training logs (generated output)
*.log

# macOS metadata
.DS_Store

# claude_automate framework (local dev only)
claude_automate/
